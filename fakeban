import discord
from discord.ext import commands

class Fakeban:
    """Fake Ban A User!"""

    def __init__(self, bot):
        self.bot = bot

    @commands.command()
    async def fban(self, ctx, user : discord.Member=None):
        """Fake Ban A User!"""

        msg = ' '
        if user!=None: 
            if iser.id == self.bot.user.id:
                user = ctx.message.author
                msg = "User has been banned! "
                await self.bot.say(user.ctx + msg)
            else:
                await self.bot.say(user.mention + msg)
        else:
            await self.bot.say (ctx.user.author.mention + msg)
        



def setup(bot):
    bot.add_cog(Fakeban(bot))

